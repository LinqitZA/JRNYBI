// Superseded by registerEChartsVisualization.ts (which builds the Renderer
// via React.createElement so the helper stays in a single .ts file). This
// re-export stub exists only so any in-flight import path keeps working.
export * from "./registerEChartsVisualization";
export { default } from "./registerEChartsVisualization";
